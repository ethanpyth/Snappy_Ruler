package com.xcelk.snappyruler.data

import com.xcelk.snappyruler.model.Shape

object Persistence {
    fun save(shape: List<Shape>) {

    }

    fun load(): List<Shape> {
        return emptyList()
    }
}